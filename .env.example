# ============================================
# Hyperliquid Trading Bot - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ==========================================
# Application
# ==========================================
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# ==========================================
# Database (PostgreSQL)
# ==========================================
POSTGRES_DB=hyperliquid
POSTGRES_USER=hyperliquid
POSTGRES_PASSWORD=your-secure-database-password-here
DB_PORT=5432

# ==========================================
# JWT Authentication
# ==========================================
# Generate a strong secret (256+ bits): openssl rand -base64 32
JWT_SECRET=your-256-bit-secret-key-here-generate-with-openssl
JWT_EXPIRATION=86400000

# ==========================================
# JVM Options (optional)
# ==========================================
JAVA_OPTS=-Xms256m -Xmx512m

# ==========================================
# Hyperliquid Credentials
# ==========================================
# IMPORTANT: Do NOT set Hyperliquid credentials here!
# Configure them via the API endpoint: POST /api/user
#
# After starting the application:
# 1. Login: POST /api/auth/login
# 2. Update user with wallet credentials: PUT /api/user/{id}
#
# This approach is more secure than environment variables
# because credentials are stored encrypted in the database.
