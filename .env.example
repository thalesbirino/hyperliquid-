# ============================================
# Hyperliquid Trading Bot - Environment Variables
# ============================================
# SETUP:
#   1. Copy to .env: cp .env.example .env
#   2. Run: ./deploy.sh
#
# NEVER commit .env to version control!

# ==========================================
# Application
# ==========================================
SERVER_PORT=8080

# ==========================================
# JWT Authentication (optional - has default)
# ==========================================
# Generate with: openssl rand -base64 32
# JWT_SECRET=your-secret-here
JWT_EXPIRATION=86400000

# ==========================================
# JVM Options (optional)
# ==========================================
JAVA_OPTS=-Xms256m -Xmx512m

# ==========================================
# Hyperliquid Wallet Credentials
# ==========================================
# DO NOT set wallet credentials here!
# Configure them via the API after deployment:
#
#   1. Login: POST /api/auth/login
#      {"username": "admin", "password": "password123"}
#
#   2. Update user with wallet: PUT /api/user/{id}
#      {"hyperliquidPrivateKey": "0x...", "hyperliquidAddress": "0x..."}
